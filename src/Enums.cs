namespace Decoherence.CommandLineParsing
{
    public enum ArgumentType
    {
        Scalar,
        Sequence,
    }
    
    public enum OptionType
    {
        Switch,
        Scalar,
        Sequence,
    }
}